version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo Installing Dependencies...
  build:
    commands:
      - echo Build started on `date`
      - echo Build completed on `date`
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - index.html
